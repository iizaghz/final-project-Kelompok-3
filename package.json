{
  "name": "kopi-senja-app",
  "version": "1.0.0",
  "description": "Kopi Senja - Sistem Self-Order QRIS, Kasir & Antrian Cerdas",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"yellow,cyan\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "concurrently -n \"BACKEND,FRONTEND\" -c \"yellow,cyan\" \"npm start --prefix backend\" \"npm run dev --prefix frontend\"",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "db:init": "npm run db:init --prefix backend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
